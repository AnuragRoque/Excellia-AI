"""Ollama client for the deferred v2 tools (``ask``, ``transform``).

Deliberately empty in v1. The LLM only assists and explains — it never
silently decides, and it must never invent numbers.
"""

from __future__ import annotations
