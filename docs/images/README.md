# Screenshots

Drop PNGs here with these exact filenames — the main [README](../../README.md) already embeds them:

| Filename | What to capture | Where from |
|---|---|---|
| `webapp-quality.png` | Quality view: profile + validate + health score | `excellia-api` → http://127.0.0.1:8000/app/ |
| `webapp-ask.png` | Ask view: answer + evidence table + the query plan | same |
| `webapp-fraud.png` | Fraud: risk bands + per-row top factors | same |
| `addin-formulas.png` | A sheet showing `=XAI.RUN` / `=XAI.VALIDATE` / `=XAI.ASK` in cells | live Excel sideload (`excellia-addin`) |
| `addin-chat.png` | Task pane chatbot answering a question over a selection | same |
| `claude-desktop.png` | Claude Desktop calling an Excellia MCP tool | Claude Desktop after pasting the config |

Tips: on Windows press `Win+Shift+S` to snip a region. Keep images under ~500 KB
(GitHub renders them inline). PNG preferred; crop tight to the content.
